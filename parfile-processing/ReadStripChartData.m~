function scdata = ReadStripChartData(filename, numchannels)
% ReadStripChartData - read strip chart data file
%
%   INPUT:
%
%   filename
%       name of the strip chart file name
%
%   frameno
%       number of channels recorded in the strip chart file (excluding the
%       date / time channel)
%
%   OUTPUT:
%
%   scdata
%       strip
%
%   NOTE:
%   1. if the image size changes, this file needs to be updated.
%   2. if the buffer contains useful information, this file needs to read
%   it in.

buffer  = 8192;

fp      = fopen(filename,'r','n');
offset  = buffer + (frameno-1)*2048*2048*2;

fseek(fp,offset,'bof');

img     = fread(fp,[2048 2048],'*uint16');

fclose(fp);
